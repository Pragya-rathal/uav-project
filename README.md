# Clustered Federated Learning with Compression

## Run
```bash
python main.py
```

Optional args:
```bash
python main.py --rounds 8 --num-devices 24 --num-clusters 4 --alpha 0.5 --topk 0.08 --qsgd-levels 8
```

Outputs are written to `results/`.
